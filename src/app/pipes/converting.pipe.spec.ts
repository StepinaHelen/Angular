import { ConvertingPipe } from './converting.pipe';

describe('ConvertingPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertingPipe();
    expect(pipe).toBeTruthy();
  });
});
